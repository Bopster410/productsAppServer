# productsAppServer
Lightweight Node.js/Express server built to provide REST API endpoints for the client application. Currently uses in-memory mock data (PostgreSQL/MongoDB integration is planned for the next iteration).
